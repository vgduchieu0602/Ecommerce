<!DOCTYPE html>
<html lang="vi">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Kích hoạt tài khoản</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      html,
      body {
        width: 100%;
        height: 100%;
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        background: linear-gradient(135deg, #667eea, #764ba2);
        display: flex;
        align-items: center;
        justify-content: center;
        overflow: hidden;
      }

      .email-wrapper {
        width: 100%;
        height: 100%;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 20px;
      }

      .email-container {
        width: 100%;
        max-width: 650px;
        background: rgba(255, 255, 255, 0.1);
        border-radius: 20px;
        backdrop-filter: blur(15px);
        box-shadow: 0 8px 40px rgba(0, 0, 0, 0.3);
        overflow: hidden;
        color: #fff;
        animation: fadeIn 1s ease-out;
      }

      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: scale(0.96);
        }
        to {
          opacity: 1;
          transform: scale(1);
        }
      }

      .header {
        text-align: center;
        padding: 40px 20px 30px;
        border-bottom: 1px solid rgba(255, 255, 255, 0.2);
      }

      .header h1 {
        font-size: 32px;
        font-weight: 700;
        margin-bottom: 10px;
      }

      .header p {
        font-size: 16px;
        opacity: 0.9;
      }

      .content {
        padding: 40px 30px;
        text-align: center;
      }

      .welcome-text {
        font-size: 20px;
        font-weight: 600;
        margin-bottom: 30px;
        color: #fff;
      }

      .activation-box {
        background: rgba(255, 255, 255, 0.15);
        border-radius: 16px;
        padding: 30px 20px;
        margin-bottom: 30px;
        box-shadow: 0 4px 25px rgba(0, 0, 0, 0.2);
        animation: pulse 2s infinite;
      }

      .activation-box h2 {
        font-size: 26px;
        margin-bottom: 10px;
      }

      .otp-code {
        display: inline-block;
        font-size: 36px;
        font-weight: 800;
        letter-spacing: 10px;
        margin: 20px 0;
        padding: 15px 25px;
        background: rgba(255, 255, 255, 0.2);
        border-radius: 12px;
        border: 2px solid rgba(255, 255, 255, 0.3);
        animation: bounce 2s infinite;
      }

      @keyframes pulse {
        0%,
        100% {
          transform: scale(1);
        }
        50% {
          transform: scale(1.03);
        }
      }

      @keyframes bounce {
        0%,
        20%,
        50%,
        80%,
        100% {
          transform: translateY(0);
        }
        40% {
          transform: translateY(-8px);
        }
        60% {
          transform: translateY(-4px);
        }
      }

      .expiry-notice {
        background: rgba(255, 255, 255, 0.1);
        border-radius: 10px;
        padding: 15px;
        margin-bottom: 30px;
        color: #ffeb99;
        font-weight: 600;
      }

      .cta-button {
        display: inline-block;
        padding: 12px 40px;
        background: linear-gradient(135deg, #667eea, #764ba2);
        color: white;
        text-decoration: none;
        font-weight: 600;
        border-radius: 50px;
        transition: all 0.3s ease;
        border: none;
        box-shadow: 0 4px 15px rgba(102, 126, 234, 0.3);
      }

      .cta-button:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 25px rgba(0, 0, 0, 0.3);
      }

      .info-section {
        text-align: center;
        margin-top: 30px;
        font-size: 15px;
        opacity: 0.85;
      }

      .info-section a {
        color: #ffda79;
        text-decoration: none;
        font-weight: 600;
      }

      .info-section a:hover {
        text-decoration: underline;
      }

      .footer {
        text-align: center;
        padding: 25px;
        font-size: 14px;
        border-top: 1px solid rgba(255, 255, 255, 0.2);
        opacity: 0.8;
      }

      @media (max-width: 600px) {
        .email-container {
          border-radius: 0;
        }

        .otp-code {
          font-size: 28px;
          letter-spacing: 6px;
        }
      }
    </style>
  </head>

  <body>
    <div class="email-wrapper">
      <div class="email-container">
        <div class="header">
          <h1>🎉 Chào mừng bạn!</h1>
          <p>Hoàn tất việc đăng ký tài khoản của bạn</p>
        </div>

        <div class="content">
          <div class="welcome-text">
            Hello <strong><%= name %></strong>,<br />
            Cảm ơn bạn đã đăng ký cùng <strong>Trung Food</strong>!
          </div>

          <div class="activation-box">
            <h2>✨ Kích hoạt tài khoản</h2>
            <p>Nhập mã dưới đây để hoàn tất đăng ký:</p>
            <div class="otp-code"><%= otp %></div>
          </div>

          <div class="expiry-notice">
            ⚠️ Mã có hiệu lực trong vòng <strong>5 phút</strong>.
          </div>

          <a href="#" class="cta-button">Kích hoạt ngay</a>

          <div class="info-section">
            Nếu bạn gặp vấn đề, vui lòng liên hệ:
            <a href="mailto:support@trungfood.com">support@trungfood.com</a>
          </div>
        </div>

        <div class="footer">
          <p>© 2025 Trung Food | Best choice for your food</p>
        </div>
      </div>
    </div>
  </body>
</html>
